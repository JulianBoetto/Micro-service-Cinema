<!DOCTYPE html>
<html lang="en" data-bs-theme="auto">
  <%- include("dashboard/head.ejs") %>
  <body>
    <%- include("buttonToggleTheme.ejs") %> <%- include("dashboard/header.ejs")
    %>

    <div class="container-fluid">
      <div class="row">
        <%- include("dashboard/sidebar.ejs") %>

        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
          <div
            class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom"
          >
            <h1 class="h2">Dashboard</h1>
            <div class="btn-toolbar mb-2 mb-md-0 dropdown">
              <!-- <div class="btn-group me-2">
                <button type="button" class="btn btn-sm btn-outline-secondary">
                  Share
                </button>
                <button type="button" class="btn btn-sm btn-outline-secondary">
                  Export
                </button>
              </div> -->
              <button
                type="button"
                class="btn btn-sm btn-outline-secondary dropdown-toggle"
                data-bs-toggle="dropdown"
                aria-expanded="false"
              >
                <span data-feather="calendar" class="align-text-bottom"></span>
                Cities
              </button>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/index?city=">All</a></li>
                
                <% cities.forEach((city) => { %>

                <li><a class="dropdown-item" href="/index?city=<%= city.cidade.toLowerCase().replaceAll(" ", "_") %>" ><%= city.cidade %></a></li>
                <% }); %>
              </ul>
            </div>
          </div>

          <h2>Movies</h2>
          <div class="table-responsive">
            <table class="table table-striped table-sm">
              <thead>
                <tr>
                  <th scope="col">#</th>
                  <!-- <th scope="col">Image</th> -->
                  <th scope="col">Title</th>
                  <th scope="col">Synopsis</th>
                  <th scope="col">Duration</th>
                  <th scope="col">Release date</th>
                  <th scope="col">Categories</th>
                </tr>
              </thead>
              <tbody>
                <% movies.forEach((movie, index) => { %>
                <tr>
                  <td><%= index + 1 %></td>
                  <!-- <td>
                    <img src="<%= movie.image %>" crossorigin="anonymous" />
                  </td> -->
                  <td><%= movie.title %></td>
                  <td><%= movie.synopsis %></td>
                  <td><%= movie.duration %></td>
                  <td>
                    <%= new Date(movie.releaseDate).toLocaleDateString() %>
                  </td>
                  <td><%= movie.categories %></td>
                </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        </main>
      </div>
    </div>

    <script src="bootstrap/js/bootstrap.bundle.min.js"></script>
    
  </body>
</html>
